MONALISA live demo - paste blocks, in order.
Paste only what is between the banner lines.

Segment C has TWO pastes (3a then 3b). Everything else is one paste each.


================================================================
BLOCK 1 - THE AUDIT                            segment A, 12 min
================================================================

There is a CSV in data/. Read it and tell me what is in it: how many rows, what
period it covers, what the columns are, and how much of each column is actually
present. Tell me anything you cannot determine from the file itself. Do not plot
anything and do not clean anything yet.

  -> then STOP. Ask the room what it did NOT ask about.
     Expect ~2 min of silence while it profiles 881 columns. Do not fill it.


================================================================
BLOCK 2 - THE GUI: list + two plots              segment B, 8 min
================================================================

Before you write any code, tell me in one paragraph what you are about to
build, so I can check you understood me. Then build it.

I want a small app for browsing this dataset:

- A list of all the available series down the left-hand side. There are hundreds
  of columns, so I need to filter that list - both by group and by typing part
  of a name - rather than scrolling one enormous dropdown.

- When I select one or more series (let me pick up to four at once), show me two
  plots on the right:
  - the time series itself, with a switch for the raw resolution, hourly and
    daily averaging
  - the mean diurnal cycle for the same series, with weekdays and weekends drawn
    separately so I can compare them

- Label the axes properly, including units where the data gives them.

Build it with Streamlit, in app.py. Keep it minimal - just what I have asked
for, no extra features.

  -> run it FROM INSIDE the live folder:
       .venv\Scripts\python.exe -m streamlit run app.py
     ~22 s to the first page. Talk over it.

     Use THAT venv, not the shared one in "My Coding\Python\.venv" - the
     shared one has no python-docx, so block 4's Word report would fail.


================================================================
BLOCK 3a - CORRELATION EXPLORER           segment C, 12 min total
================================================================

Add a correlation panel. I pick one compound, and you rank every other series
in the dataset by how strongly it correlates with it.

- On the left, the twenty strongest correlations: a horizontal bar chart of
  Pearson r, and a table of the same twenty showing r and R squared. Let me
  select a row of that table.

- On the right, the pair I have selected: a scatter plot coloured by hour of
  day, with a least-squares fit line drawn on it, and the statistics printed -
  slope, intercept, Pearson r, R squared, Spearman, and n.

Let me search all the series when choosing the compound, not just the ones
filtered in the sidebar.

Keep it minimal - no extra features.

  -> DEMO gas_NO, not toluene. Type "NO" in the compound box.
     Top hits: NOX 0.88, then D5 siloxane 0.70, eBCff 0.69, toluene 0.68,
     eBCwb 0.66, CO 0.66 - a recognisable traffic-emissions cluster, and every
     label means something to this audience.

     EXPECT A WARNING on the first pair (NO vs NOX): Pearson 0.880 against
     Spearman 0.626. NO is heavily right-skewed, so Pearson rides on a few
     traffic spikes while Spearman reflects the typical ordering. Good moment,
     but it arrives unprompted so be ready for it.

  -> THEN switch the compound to toluene (type C7H9) for the punchline.
     All twenty are other PTR ions from the same instrument: C7H8 at 0.989 is
     essentially the same molecule, then C5H11, C4H9, C6H13 - alkyl fragments.
     Twenty bars between 0.93 and 0.99 and not one recognisable name. Say:

       "Rank everything by correlation and the top twenty are my own
        instrument measuring pieces of the same molecule. That 0.99 is
        telling me about the spectrometer, not the atmosphere."

  -> AND the note-worthy science: benzene (C6H7) vs monoterpenes (C10H17),
     r = 0.86. Do they share a source? No - 0.95 at night against 0.50 at
     midday, and both anticorrelate with wind speed. Shared dilution, not
     shared chemistry. THAT is the argument to write down in 3b.


================================================================
BLOCK 3b - NOTES                             segment C, same 12 min
================================================================

Add a box where I can type a note about whatever I am currently looking at,
together with who said it - and make it work in both places: against a selected
series in the time-series panel, and against the pair I have chosen in the
correlation panel. Save every note to a JSON file on disk the moment I press
save, organised one entry per series or pair, so that restarting the app cannot
lose anything. Show the notes for the current selection underneath the plots.

Keep it minimal - no extra features.

  -> now take the correlation argument from the room and type it in VERBATIM.
     Good asks:
       "So what should we write down about that correlation?"
       "Which number in that argument would you check first, and how?"
     Then show them notes.json in the editor.


================================================================
BLOCK 4 - THE WORD REPORT                        segment D, 4 min
================================================================

Add a button that turns those notes into a Word document: one section per
series or pair with its notes and some basic statistics, the plots that are
currently on screen, and a provenance section recording which file the data
came from, what period it covers, and how it was processed.

Keep it minimal - no extra features.

  -> first, drop the prompt log in so provenance is populated:
     Copy-Item "$env:USERPROFILE\Desktop\My Folders\My Coding\Python\GitHub\FZJ-Condenses-Course-on-AI\prep\prompts_for_report.json" ".\prompts.json"
  -> open the .docx and scroll to Provenance.


================================================================
BLOCK 5 - THE REVEAL                             segment E, 4 min
================================================================

We started this hour with one CSV and three lines of text. Write two documents
for what we have built:

- README.md - what this tool does, how to run it, and what decisions you made
  while building it. Be specific about how you averaged each kind of column and
  what you did with missing data.

- ARCHITECTURE.md - how the pieces fit together. Include a diagram, drawn in
  plain text, showing how the data flows from the CSV through to the plots, the
  notes file and the Word report.

  -> have File Explorer visible. They watch the tree fill up.
     README is asked for FIRST on purpose: it carries the judgement calls, so
     if you run out of time mid-generation that is the one you want.
     Read two of those decisions out loud - the thesis, in its own words.
     Then put the ARCHITECTURE diagram on screen as the last thing they see.
     If the README is vague about decisions, push once:
       be specific about how you averaged each kind of column.


================================================================
IF YOU ARE RUNNING OVER - cut in this order
================================================================

Budget: 12 + 8 + 12 + 4 + 4 = 40 min, which is the agreed slot. Merging
correlations into the notes segment removed the designed casualty, so there is
no longer a whole segment to drop. Cut in this order instead:

  1. ARCHITECTURE.md in block 5 - ask for README.md only.        saves ~1.5 min
  2. Take one audience note in 3b instead of two or three.       saves ~2 min
  3. Skip the B1 menu - pick option 1 (toluene + gas_NO) yourself. saves ~2 min
  4. In 3a, skip the toluene punchline and keep only gas_NO.     saves ~2 min

That takes 40 down to about 32.

Never cut: block 1 (the audit is the point), the benzene/monoterpene argument
in 3a, or the README.

If block 3a itself stalls - it is the most ambitious build in the demo - do not
debug it live. Switch to the prepared branch:

  git -C ..\MONALISA_fallback checkout -q stage-3a-correlations
  Copy-Item -Force ..\MONALISA_fallback\app.py .


================================================================
CONTINGENCY - only if it used a plain mean on wind direction
================================================================

In the trial it got this right on its own and used a vector mean. If a run does
NOT, this is the fix:

Wind direction is a circular variable - you cannot take an arithmetic mean of
degrees, because 350 and 10 average to 180 instead of 0. Use a vector mean
instead: average the sine and the cosine and take the arctangent. Fix the
diurnal plot, and record in the app and in the report which averaging method is
being used.


================================================================
ONE-PASTE FALLBACK - only if badly behind
================================================================

Loses every pause and gives one long silent build with no partial result.

Read the CSV in data/, then build a Streamlit app for browsing it. A filterable
list of all the series down the left. When I select up to four, show the time
series and the mean diurnal cycle with weekdays and weekends separately. A
separate panel for correlating any two series, coloured by hour of day. A box to
type notes against either a selected series or a correlation pair, appended to a
JSON file on disk immediately. A button that builds a Word document from those
notes with the figures, basic statistics and a provenance section. Keep it
minimal.
